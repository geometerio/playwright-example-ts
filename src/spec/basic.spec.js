import * as playwright from "playwright";


// const { addAttach } = require('jest-html-reporters/helper');

// describe('Loading google.com', () => {
//   it('matches some text', async() => {
//     try {
//       await page.goto('https://google.com/');
//       await expect(page).toHaveText('Carbon neutral');

//       const screenshot = await page.screenshot();
//       await addAttach(screenshot, 'Screenshot at success');
//   } catch(e) {
//       const screenshot = await page.screenshot();
//       await addAttach(screenshot, 'Screenshot at failure');
//       throw e;
//     }
//    })
// });
